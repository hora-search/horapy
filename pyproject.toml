[build-system]
requires = ["maturin>=0.11,<0.12"]
build-backend = "maturin"

[project]
name = "horapy"
dependencies = ["numpy>=1.6.0"]
classifiers = [
    "Programming Language :: Python",
    "Programming Language :: Rust"
]
